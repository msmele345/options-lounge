spring:
  jpa:
#    database: MYSQL
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        globally_quoted_identifiers: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver